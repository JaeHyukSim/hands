<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.bangkoklab.data.repository.mapper.AuthTimerMapper">

	<select id="isExistedTimer" resultType="int"
		parameterType="String">
		SELECT COUNT(*) as "count"
		FROM auth_timers
		WHERE email=
		#{email}
	</select>

	<update id="updateTimerByEmail" parameterType="String">
		UPDATE auth_timers
		SET start_timer = now()
		WHERE email=#{email}
	</update>

	<insert id="addTimerByEmail" parameterType="String">
		INSERT INTO
		auth_timers (
		email,
		start_timer
		) VALUES (
		#{email},
		now()
		)
	</insert>

	<select id="getDiffTime" parameterType="String"
		resultType="int">
		SELECT TIMESTAMPDIFF(MINUTE, start_timer, now())
		FROM auth_timers
		WHERE email=#{email}
	</select>

</mapper>